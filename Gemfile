# frozen_string_literal: true
source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# brew install opencv --with-contrib
# bundle config build.ruby-opencv --with-opencv-dir=/usr/local/Cellar/opencv/3.3.0_3

# brew install opencv@2
# bundle config build.ruby-opencv --with-opencv-dir=/usr/local/opt/opencv@2

gem "ruby-opencv", require: 'opencv'
gem "pry-byebug"
